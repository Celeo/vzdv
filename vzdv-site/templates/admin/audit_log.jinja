
{% extends "_layout.jinja" %}

{% block title %}Audit log | {{ super() }}{% endblock %}

{% block body %}

<h2 class="pb-3">Audit log</h2>

<p>
  This page lists notable events across the site for later view.<br>
  Note that some of these messages aren't formatted with controller names or similar; some manual linking
  may be required to investigate events.
</p>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Date</th>
      <th>Message</th>
    </tr>
  </thead>
  <tbody>
    {% for log in logs %}
      <tr>
        <td>{{ log.created_date|nice_date }}</td>
        <td>{{ log.message }}</td>
      </tr>
    {% endfor %}
  </tbody>
</table>

{% endblock %}
